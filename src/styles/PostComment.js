import styled from "styled-components";
export const PostComment = styled.div`
  margin: 1em 0;
  display: flex;
  align-self: flex-end;
  align-items: flex-start;

  & :first-child {
    padding-right: 0.5em;
  }
`;
